import { Meteor } from 'meteor/meteor';
import {Leaderboard} from './../imports/api/leaderboard';
import {Battle} from "../imports/api/battle";
import {Leaderboardhero} from "../imports/api/leaderboardhero";

Meteor.startup(() => {
  // code to run on server at startup
});
